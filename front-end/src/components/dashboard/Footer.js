import React from "react";
import { Layout, Typography } from "antd";
const Footer = () => {
  return (
    <Layout.Footer style={{ padding: 16, backgroundColor: "#fff" }}>
      <Typography.Title level={5}>Footer</Typography.Title>
    </Layout.Footer>
  );
};

export default Footer;
